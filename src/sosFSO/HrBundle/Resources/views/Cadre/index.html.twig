{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Cadre list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Intitule</th>
                <th>Echelle</th>
                <th>Description</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('cadre_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.intitule }}</td>
                <td>{{ entity.echelle }}</td>
                <td>{{ entity.description }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('cadre_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('cadre_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('cadre_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
